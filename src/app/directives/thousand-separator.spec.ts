import { ThousandSeparator } from './thousand-separator';

describe('ThousandSeparator', () => {
  it('should create an instance', () => {
    const directive = new ThousandSeparator();
    expect(directive).toBeTruthy();
  });
});
