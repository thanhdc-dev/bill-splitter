export interface SettingsData {
  bankAccount: {
    bankBin: string;
    accountName: string;
    accountNumber: string;
  };
  momoWallet: {
    accountName: string;
    accountNumber: string;
    phoneNumber: string;
  };
};
